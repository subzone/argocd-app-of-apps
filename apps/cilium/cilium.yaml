kind: Namespace
apiVersion: v1
metadata:
  name: cilium
  labels:
    name: cilium
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cilium
  namespace: argocd
spec:
  project: argo-app-of-apps
  sources:
    - repoURL: 'git@github.com:subzone/argocd-app-of-apps.git'
      targetRevision: main
      ref: argo-repo
    - repoURL: 'https://github.com/cilium/charts'
      chart: cilium/cilium
      targetRevision: 1.16.4 # Update this to a valid version
      helm:
        valueFiles:
          - $argo-repo/apps/cilium/values/values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: cilium
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  syncOptions:
    - CreateNamespace=true
  hooks:
    - type: PreSync
      resource:
        apiVersion: v1
        kind: Namespace
        metadata:
          name: kube-system